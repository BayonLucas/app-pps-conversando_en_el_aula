import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-conversando-en-el-aula',
  webDir: 'www'
};

export default config;
